[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "acc"
version = "0.1.0"
dependencies = [
    "torch",
    "numpy",
    "loguru",
    "xxhash",
]

[tool.setuptools.packages]
find = { include = ["acc"] }
